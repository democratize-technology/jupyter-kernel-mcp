[project]
name = "jupyter-kernel-mcp"
version = "0.1.0"
description = "MCP server for Jupyter kernel execution"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "httpx",
    "websockets",
]